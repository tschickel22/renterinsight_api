name: CI
on: [push, pull_request]

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # System packages many Rails gems need during native builds
      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential pkg-config \
            libsqlite3-dev libpq-dev

      - uses: ruby/setup-ruby@v1
        with:
          # Read the pin from .ruby-version so it can't drift from the Gemfile.
          # ('3.2' floated to 3.2.11 and broke bundle install against the 3.2.6 pin.)
          ruby-version: .ruby-version
          bundler-cache: true

      - name: Bundle install
        run: bundle install --jobs 4 --retry 3

      - name: Print Ruby/Rails versions
        run: |
          ruby -v
          bundle exec rails -v || true

      - name: List rake tasks (optional)
        run: bundle exec rake -T || true
