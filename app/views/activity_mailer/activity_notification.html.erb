<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
    <style>
      body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
      .container { max-width: 600px; margin: 0 auto; padding: 20px; }
      .header { background-color: #3b82f6; color: white; padding: 20px; border-radius: 5px 5px 0 0; }
      .content { background-color: #f9fafb; padding: 30px; border: 1px solid #e5e7eb; }
      .activity-details { background-color: white; padding: 20px; border-radius: 5px; margin: 20px 0; }
      .detail-row { margin: 10px 0; }
      .label { font-weight: bold; color: #6b7280; }
      .value { color: #111827; }
      .priority-urgent { color: #ef4444; font-weight: bold; }
      .priority-high { color: #f59e0b; font-weight: bold; }
      .priority-medium { color: #3b82f6; }
      .priority-low { color: #6b7280; }
      .button { background-color: #3b82f6; color: white; padding: 12px 24px; text-decoration: none; border-radius: 5px; display: inline-block; margin: 20px 0; }
      .footer { text-align: center; color: #6b7280; font-size: 12px; margin-top: 30px; }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header">
        <h1 style="margin: 0;"><%= activity_type_label %></h1>
      </div>
      
      <div class="content">
        <p>Hi <%= @user.name %>,</p>
        
        <p>You have a new activity assigned to you:</p>
        
        <div class="activity-details">
          <div class="detail-row">
            <span class="label">Subject:</span>
            <span class="value"><%= @activity.subject %></span>
          </div>
          
          <% if @activity.description.present? %>
          <div class="detail-row">
            <span class="label">Description:</span>
            <div class="value" style="margin-top: 5px;"><%= simple_format(@activity.description) %></div>
          </div>
          <% end %>
          
          <div class="detail-row">
            <span class="label">Priority:</span>
            <span class="value priority-<%= @activity.priority %>"><%= @activity.priority.titleize %></span>
          </div>
          
          <% if @activity.due_date.present? %>
          <div class="detail-row">
            <span class="label">Due Date:</span>
            <span class="value"><%= @activity.due_date.strftime("%B %d, %Y at %I:%M %p") %></span>
          </div>
          <% end %>
          
          <% if @activity.activity_type == 'meeting' && @activity.start_time.present? %>
          <div class="detail-row">
            <span class="label">Meeting Time:</span>
            <span class="value"><%= @activity.start_time.strftime("%B %d, %Y at %I:%M %p") %></span>
          </div>
          <% if @activity.meeting_location.present? %>
          <div class="detail-row">
            <span class="label">Location:</span>
            <span class="value"><%= @activity.meeting_location %></span>
          </div>
          <% end %>
          <% if @activity.meeting_link.present? %>
          <div class="detail-row">
            <span class="label">Meeting Link:</span>
            <span class="value"><a href="<%= @activity.meeting_link %>"><%= @activity.meeting_link %></a></span>
          </div>
          <% end %>
          <% end %>
          
          <% if @activity.activity_type == 'call' && @activity.phone_number.present? %>
          <div class="detail-row">
            <span class="label">Phone Number:</span>
            <span class="value"><%= @activity.phone_number %></span>
          </div>
          <% end %>
          
          <div class="detail-row">
            <span class="label">Related to:</span>
            <span class="value"><%= @lead.first_name %> <%= @lead.last_name %></span>
          </div>
        </div>
        
        <a href="<%= "http://localhost:3000/crm/leads/#{@lead.id}?tab=activities" %>" class="button">
          View in CRM
        </a>
        
        <p style="color: #6b7280; font-size: 14px; margin-top: 30px;">
          This is an automated notification from your CRM system.
        </p>
      </div>
      
      <div class="footer">
        <p>RenterInsight CRM</p>
        <p>To manage your notification preferences, visit your account settings.</p>
      </div>
    </div>
  </body>
</html>
